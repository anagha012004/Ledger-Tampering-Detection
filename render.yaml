services:
  - type: web
    name: ledger-tampering-detection
    runtime: docker
    envVars:
      - key: JWT_SECRET
        sync: false
      - key: MONGODB_URI
        sync: false
      - key: MAIL_ENABLED
        value: "false"
