services:
  - type: web
    name: ledger-tampering-detection
    runtime: docker
    envVars:
      - key: JWT_SECRET
        sync: false
      - key: MONGODB_URI
        sync: false
      - key: PORT
        value: "10000"
      - key: BRIDGE_PORT
        value: "3001"
